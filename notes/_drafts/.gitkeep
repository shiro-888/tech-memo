# 未完成の記事は <slug>.html としてこのディレクトリに直置きする。
# index.html からはリンクしないこと（誤公開防止 / AGENTS.md 参照）。
# 完成したら notes/<公開日-スラッグ>/index.html へ移動し、index.html にリンクを追加する。
# テンプレート・下書き・公開ノートはいずれも 2 階層下なので CSS パスは ../../ のまま。
