version: 2
updates:
  # GitHub Actions の更新を自動追従する。
  # SHA 固定したアクションも、Dependabot が SHA と末尾の版数コメントを更新する。
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    commit-message:
      prefix: "ci"
    labels:
      - "dependencies"
